#include "main.h"
/**
 *_islower-checks if character is lower case
 * @c: a character argument
 * Return:return 0 and 1 depending on argument
 */
int _islower(int c)
	{
		return (c >= 'a' && c <= 'z');
	}
