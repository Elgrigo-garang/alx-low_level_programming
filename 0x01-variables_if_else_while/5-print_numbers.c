#include<stdio.h>
/**
 * main-prints all single digit numbers of base 10
 *
 * Return:return 0
 */
int main(void)
	{
		int a;

		for (a = 0; a < 10; a++)
		{
			printf("%i", i);
		}
		printf("\n");
		return (0);
	}
