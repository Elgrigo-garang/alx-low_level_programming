#include "main.h"
/**
 * add - afunction that adds two integers and returns the result
 * @a:first number input
 * @b:second number input
 * Return:one + two
 */
int add(int a, int b)
	{
		return (a + b);
	}
